
#
# Note:
# Only used by TFS remote build script to determine build tools to use.
#
set(SSP_BUILDTOOLS_VERSION "current")

#
# Note:
# Only used by TFS remote build script to determine default PSP to use.
#
set(SSP_PSP_REFERENCE "mecha1-psp-V7.1.0.10")

####### PROJECT LIBRARIES ##################################################################################

set(SSP_ICU_NAMEVERSION "icu-V52.1.0.4")

set(SSP_CUPS_NAMEVERSION "cups-V1.5.4.7")

